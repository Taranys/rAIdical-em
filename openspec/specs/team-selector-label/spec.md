### Requirement: Team selector default option shows "Choose a team"
The team filter dropdown in the GitHub import sheet SHALL display "Choose a team" as the default option label when no team is selected.

#### Scenario: Default state of team selector
- **WHEN** the GitHub import sheet is opened and teams are available
- **THEN** the team selector dropdown displays "Choose a team" as the first option
