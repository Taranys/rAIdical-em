## 1. Layout Change

- [ ] 1.1 In `src/app/period-selector.tsx`, change the container div className from `flex flex-col items-end gap-1` to `flex flex-row items-center gap-3`
- [ ] 1.2 Verify the date range label `<p>` element remains after the `<Select>` component (right side in row layout)

## 2. Testing

- [ ] 2.1 Run existing unit tests to ensure no regressions
- [ ] 2.2 Visually verify the PeriodSelector renders with the label to the right of the dropdown
